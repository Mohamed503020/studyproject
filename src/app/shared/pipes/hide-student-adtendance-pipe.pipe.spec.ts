/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HideStudentAdtendancePipePipe } from './hide-student-adtendance-pipe.pipe';

describe('Pipe: HideStudentAdtendancePipee', () => {
  it('create an instance', () => {
    let pipe = new HideStudentAdtendancePipePipe();
    expect(pipe).toBeTruthy();
  });
});
